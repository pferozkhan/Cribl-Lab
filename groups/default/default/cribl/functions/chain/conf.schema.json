{
  "type": "object",
  "title": "",
  "required": ["processor"],
  "properties": {
    "processor": {
      "title": "Processor",
      "description": "The data processor (Pack/Pipeline) to send events through",
      "type": "string"
    }
  }
}